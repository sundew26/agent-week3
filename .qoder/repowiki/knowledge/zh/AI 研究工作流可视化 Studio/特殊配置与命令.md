后端需 `cd workflow-studio/backend && pip install -r requirements.txt && cp .env.example .env` 并填入 `OPENAI_API_KEY`，再执行 `uvicorn app.main:app --reload --port 1994`；前端需 `cd workflow-studio/frontend && npm install && npm run dev` 启动于端口 1993。